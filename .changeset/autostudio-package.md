---
"@jmfederico/pi-web": patch
---

Add Autostudio manager commands with persistent chat progress and links to live Pi Web worker sessions. Delegate through pi-subagents, preserve the manager workspace and recent conversation context, and retain roadmap, review and retry state under `.autostudio/`.
